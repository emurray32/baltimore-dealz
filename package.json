{
  "name": "baltimore-dealz",
  "version": "0.1.0",
  "private": true,
  "description": "Tonight in Canton — a board of Baltimore bar/restaurant specials.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "build": "node scripts/build-static.mjs",
    "test": "node --test \"test/*.test.js\"",
    "check-sources": "node scripts/check-sources.mjs"
  }
}
